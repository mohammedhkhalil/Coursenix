@model Coursenix.Models.StudentDashboardViewModel

@{
    Layout = "_Layout";
}
@section css{
    <link rel="stylesheet" href="/css/student-dashboard.css">
}

<!-- Subheader -->
<div class="subheader">
    <ul class="sub-nav">
        <li><a href="#" class="active">Courses</a></li>
        <li><a href=@Url.Action("Settings", "Student") >Settings</a></li>
    </ul>
</div>
<main>
    <h2 class="page-title">My Courses</h2>

    <div class="courses-grid" id="coursesGrid">
        @foreach (var course in Model.Courses)
        {
            <div class="course-card" data-course="@course.CourseName.ToLower().Replace(" ", "-")">
                <div class="course-header">
                    <h3>@course.CourseName</h3>
                    <div class="teacher">
                        <div class="teacher-avatar"></div>
                        <span class="teacher-name">@course.TeacherName</span>
                    </div>
                </div>
                <div class="course-content">
                    <div class="course-info">
                        <div class="course-info-item">
                            <i class="icon group-icon"></i>
                            <div class="course-info-label">Group:</div>
                            <div class="course-info-value">@course.GroupName</div>
                        </div>
                        <div class="course-info-item">
                            <i class="icon location-icon"></i>
                            <div class="course-info-label">Location:</div>
                            <div class="course-info-value">@course.Location</div>
                        </div>
                        <div class="course-info-item">
                            <i class="icon days-icon"></i>
                            <div class="course-info-label">Days:</div>
                            <div class="course-info-value">@course.Days</div>
                        </div>
                        <div class="course-info-item">
                            <i class="icon time-icon"></i>
                            <div class="course-info-label">Time:</div>
                            <div class="course-info-value">@course.Time</div>
                        </div>
                        <div class="course-info-item">
                            <i class="icon absence-icon"></i>
                            <div class="course-info-label">Absence:</div>
                            <div class="course-info-value">
                                <span class="absence-ratio @course.AbsenceClass">@course.AbsenceRatio%</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</main>

@section Scripts{
    <script src="/js/student-dashboard.js"></script>
}